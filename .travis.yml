sudo: false
language: python
python:
        - 2.7
        - 3.4
install:
        - python setup.py develop
        - pip install -r requirements-test.txt
        - pip install python-coveralls
script: py.test tests --cov network_tester --durations=10
after_success:
        - coveralls
notifications:
        email: false
